## Слайд 2 — С какой маржой Atlas Cloud продает DeepSeek V4 Pro

Ссылка: <https://www.atlascloud.ai/models/deepseek-ai/deepseek-v4-pro>
> «Atlas Cloud продает DeepSeek V4 Pro по $1.68 за миллион входных токенов и $3.38 за миллион выходных. Они на этом зарабатывают, работают в ноль или вообще в минус?»

## Что прислать

1. Два слайда в PDF (по одному на каждый сюжет).
2. Ссылку на код / репозиторий / Notebook , по которым можно воспроизвести расчеты и понять, откуда взялись цифры.

## Ответ
зарабатывают. Валовая маржа на вычислениях ≈ 50% (диапазон 25–70%),   
себестоимость ≈ $0.6–1.2 за 1M токенов против выручки $2.02 за 1M на типичном миксе.   
<Но это маржа на железе, а не софтверная: сам DeepSeek продаёт те же веса в 4 раза дешевле.>   

## Решение:

Логика:   
считать надо не «цену за токен», а экономику одного GPU-узла в час. 

Модель весит около 865 ГБ, поэтому минимальная конфигурация — один узел 8×B200 (Lambda). Замеренная на нем пропускная способность vLLM — 912 токенов/с на выход и 3 650 на вход. За час это 3,3 млн выходных и 13,1 млн входных токенов, то есть $33 выручки с узла в час, или $2,02 за 1 млн токенов в среднем по миксу.    
Проверка: DeepInfra публикует рыночную blended-цену этой модели как $2,17 за 1M (бенчмарк по 6 провайдерам) — сходится.

Аренда B200 стоит от $2,5 (спот) до $6,1 (медиана он-деманда) за GPU-час (Beam Cloud, трекер ставок), то есть $20–49 за узел. При настроенном сервинге себестоимость выходит $0,6–1,2 за 1 млн токенов, валовая маржа около 50%, безубыточность — при загрузке 31% на резерве.

В минус можно уйти только сочетанием трех ошибок сразу: ненастроенный сервинг, GPU по медианной цене он-деманда и загрузка ниже половины.

# 0. Зафиксировать цену и продукт
|Параметр|Значение|
|---|---|
|Цена входа|$1.68 / 1M токенов|
|Цена выхода|$3.38 / 1M токенов|
|Контекст|1 048 576 токенов|
|Макс. выход|393 216 токенов|
|Модель тарификации|pay-as-you-go, без минимума|    
   

Источник: страница модели на Atlas Cloud: https://www.atlascloud.ai/models/deepseek-ai/deepseek-v4-pro   
Важно: цена за выход ровно в 2 раза выше цены за вход. Это отраслевая конвенция, а не отражение реальной себестоимости — вход обрабатывается на порядок дешевле выхода (префилл считается батчем и параллелится, декодирование — последовательное и ограничено пропускной способностью памяти). 
Отсюда следует ключевой вывод шага 3: экономика провайдера почти целиком определяется миксом входных и выходных токенов в трафике.

# 1. Понять, что за модель мы обслуживаем

| Параметр | Значение | Источник |
|---|---|---|
| Всего параметров | 1.6 трлн | Источник1 |
| Активных на токен | 49 млрд (MoE) | Источник1 |
| Размер весов | ~865 ГБ (MoE в FP4, остальное FP8) | Источник2 |
| Внимание | гибрид CSA + HCA | Источник3 |
| FLOPs на токен при 1M контекста | 27% от V3.2 | Источник3 |
| KV-кэш при 1M контекста | 10% от V3.2 | Источник3 |
| Лицензия | MIT, коммерческое использование разрешено | Источник4 |

источник1 - https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro   
источник2 - https://framia.converge.ai/page/en-US/news/deepseek-v4-parameters   
источник3 - https://docs.api.nvidia.com/nim/reference/deepseek-ai-deepseek-v4-pro   
источник4 - https://llm-stats.com/models/deepseek-v4-pro-max

Три следствия для экономики:

1. 49B активных, а не 1.6T. Считать себестоимость по полному размеру модели — грубая ошибка. Арифметика инференса определяется активными параметрами; 1.6T влияют только на требования к памяти.
2. 865 ГБ весов задают минимальную конфигурацию. Один узел 8×B200 (1 536 ГБ HBM) — это минимум, на котором модель вообще загружается. Меньше нельзя, значит нижняя граница затрат — стоимость целого узла.
3. 10% KV-кэша от V3.2 — это и есть источник маржи на длинном контексте: именно KV-кэш обычно ограничивает размер батча, а значит и себестоимость токена.

# 2. Взять эталонную пропускную способность

Нужны измеренные токены в секунду на известном железе. Публичный бенчмарк vLLM от Lambda
на DeepSeek V4 Pro (https://lambda.ai/inference-models/deepseek-ai/deepseek-v4-pro):

| Метрика | Значение |
|---|---|
| Железо | 1 узел NVIDIA HGX B200 (8 GPU) |
| Нагрузка | 8 192 вход / 2 048 выход, микс 4:1 |
| Параллельных запросов | 32 |
| Генерация (выход) | 911.92 tok/s |
| Всего (вход + выход) | 4 561.38 tok/s |
| На пользователя | 28.5 tok/s |
| TTFT (среднее) | 1 186 мс |

Отсюда вход = 4 561.38 − 911.92 = **3 649.5 tok/s**.

Проверка вменяемости: 28.5 tok/s на пользователя совпадает с тем, что независимо измеряют у живых провайдеров — 33–41 tok/s у DeepInfra, Novita, Together и самого DeepSeek (https://deepinfra.com/blog/deepseek-v4-pro-max-api-benchmarks-latency-throughput-cost).
Значит бенчмарк Lambda — не синтетический идеал, а примерно то, что стоит в продакшене.

Оговорка: 32 параллельных запроса — это мало. Продовое обслуживание идёт на батчах в сотни запросов, с разделением префилла и декодирования по разным пулам GPU. Fireworks на этой же модели выдаёт 167 tok/s на пользователя — в 5 раз быстрее остальных, что показывает разброс качества инженерии. Поэтому вводим множитель настройки.

## Шаг 3. Взять стоимость GPU-часа

Публичные ставки на B200 за июль–август 2026, за 1 GPU в час:

| Уровень | $/GPU-час | Источник |
|---|---|---|
| Спот | $2.12–2.74 | https://www.beam.cloud/blog/nvidia-b200-pricing |
| Дешёвые неклауды, он-деманд | $3.70–3.75 | https://www.spheron.network/blog/nvidia-b200-cloud-pricing-2026/   https://packet.ai/blog/b200-gpu-cloud-pricing-specs |
| Медиана рынка он-деманд | $6.12 | https://smarttec.dev/market |
| AWS p6-b200, листовая | $14.24 | [там же](https://smarttec.dev/market) |

Для модели берём три уровня: спот $2.50, годовой резерв $3.20, он-деманд $3.75 и медиану $6.12.
Реальный провайдер такого масштаба не платит медиану он-деманда — он сидит на резерве или на собственном железе, поэтому центральный сценарий — резерв.

Узел = 8 GPU, значит затраты на узел в час = ставка × 8.

# 4. Свести выручку и затраты на одном узле

Формула:

```
выход_млн_ток_час  = OUT_TPS × множитель × 3600 / 1e6
вход_млн_ток_час   = IN_TPS  × множитель × 3600 / 1e6
выручка_час        = вход × $1.68 + выход × $3.38
затраты_час        = ставка_GPU × 8
себестоимость_1М   = затраты_час / (вход + выход)
маржа              = (выручка − затраты) / выручка
```

При множителе 1.0 (ровно бенчмарк Lambda):

- выход: 3.28 млн токенов/час → $11.10
- вход: 13.14 млн токенов/час → $22.07
- **выручка $33.17 в час с узла**, blended **$2.02 за 1M токенов**

Проверка вменяемости: DeepInfra публикует blended-цену этой модели по рынку как **$2.17 за 1M токенов**.
Наши $2.02 при цене Atlas, которая на 3% ниже рыночной, — сходится. Модель не сломана.

### Результаты по сценариям

Маржа на вычислениях при 100% утилизации:

| Настройка | спот $2.50 | резерв $3.20 | он-деманд $3.75 | медиана $6.12 |
|---|---|---|---|---|
| A. как в бенчмарке (×1.0) | +40% | +23% | +10% | −48% |
| B. продовая настройка (×2.5) | +76% | +69% | +64% | +41% |
| C. агрессивная оптимизация (×4.0) | +85% | +81% | +77% | +63% |

Себестоимость 1M токенов (blended) в тех же клетках: от $0.30 до $2.98.

## Шаг 5. Добавить утилизацию — то, что решает всё

GPU арендуется круглосуточно, а трафик неровный. Поэтому реальный ответ определяется
не себестоимостью токена, а долей часов, когда узел загружен. Точка безубыточности:

| Настройка | спот | резерв | он-деманд | медиана |
|---|---|---|---|---|
| A (×1.0) | 60% | 77% | 90% | не выходит |
| B (×2.5) | 24% | 31% | 36% | 59% |

**Центральный сценарий** — продовая настройка, резерв $3.20/GPU-час, утилизация 60%:
выручка $49.8/час с узла, аренда $25.6/час, себестоимость **$1.04 за 1M токенов**,
**валовая маржа 49%**.

---

## Ответ

**Зарабатывают.** На типичном миксе трафика узел приносит примерно вдвое больше,
чем стоит его аренда. Порог безубыточности при нормальной инженерии — около 30% загрузки,
что достигается легко. В минус можно уйти только сочетанием трёх ошибок сразу:
ненастроенный сервинг, GPU по медианной цене он-деманда и загрузка ниже половины.

Три уточнения, которые важнее самой цифры маржи:

1. **Маржу делает вход, а не выход.** На миксе 4:1 две трети выручки приходят с входных токенов,
   которые обрабатываются в разы дешевле выходных. Агентский и long-context трафик,
   на который нацелен 1M-контекст, — самый выгодный для провайдера.
   Чат-трафик с коротким промптом и длинным ответом — самый убыточный.
2. **Это маржа на железе, а не софтверная.** Она не 90%, как у закрытых лабораторий,
   и она полностью зависит от ставки аренды и утилизации — двух вещей, которые ходят на десятки процентов.
3. **Главный риск — не себестоимость, а цена.** Atlas стоит на рыночном уровне: список у DeepInfra,
   Fireworks, Novita и Together — $1.74/$3.48, у Novita $1.60/$3.20, у Atlas $1.68/$3.38.
   Но сам DeepSeek после снижения цены на 75% продаёт V4 Pro примерно за **$0.44/$0.88**
   ([InfoWorld](https://www.infoworld.com/article/4176709/deepseeks-steep-v4-pro-price-cut-escalates-ai-pricing-war.html)),
   то есть вчетверо дешевле. Веса открыты по MIT, барьеров входа нет.
   Сегодняшняя маржа держится на удобстве, латентности и SLA, а не на структурном преимуществе.
   Один шаг конкурента по цене — и вся вилка из шага 4 сдвигается в отрицательную зону.

## Границы точности

- Множитель продовой настройки (×2.5) — оценка, а не измерение. Публичных данных о батчинге
  и дизагрегации у Atlas нет. Разброс ×1–4 покрыт таблицей.
- Не учтены: стоимость простаивающего резерва под пики, трафик и хранение, зарплаты,
  оплата за неудавшиеся запросы, экономия на кэш-хитах входа (последнее работает в плюс провайдеру).
- Микс 4:1 взят из бенчмарка. При миксе 1:1 blended-выручка падает примерно на треть,
  и вся таблица маржи сдвигается вниз на 10–15 пунктов.
- Ставки аренды GPU за июль–август 2026; за квартал они двигались на 15–18%.

## Приложение: воспроизводимый расчёт

Скрипт `atlas_econ.py` в рабочей папке. Ядро расчёта:

```python
P_IN, P_OUT = 1.68, 3.38
OUT_TPS, TOT_TPS = 911.92, 4561.38     # Lambda, HGX B200, 32 параллельных
IN_TPS = TOT_TPS - OUT_TPS
GPUS = 8

def econ(mult, gpu_hr, util=1.0):
    out_h = OUT_TPS * mult * 3600 / 1e6
    in_h  = IN_TPS  * mult * 3600 / 1e6
    rev   = (in_h * P_IN + out_h * P_OUT) * util
    cost  = gpu_hr * GPUS
    tok   = (in_h + out_h) * util
    return rev, cost, cost / tok, (rev - cost) / rev

econ(2.5, 3.20, util=0.60)   # -> выручка 49.8, аренда 25.6, $1.04/1M, маржа 49%
rev, cost, cpm, margin = econ(2.5, 3.20, util=0.60)
print(f"выручка ${rev:.2f}/ч | аренда ${cost:.2f}/ч | ${cpm:.3f}/1M | маржа {margin:.1%}")
```
